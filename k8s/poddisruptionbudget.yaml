apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: ci-cd-demo-app
  namespace: default
  labels:
    app: ci-cd-demo-app
    managed-by: ci-cd-pipeline
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: ci-cd-demo-app
